fib :: ent n -> ent { 
    si (n < 0) {
        escribir "Error: n tiene que ser >= 0"
    } osi ((n == 0) | ( n == 1)) {
        ent a
        retorna 1
    } 
    retorna fib(n-1) + fib(n-2) 
}

enie :: ~ {
    fib(10)
}

